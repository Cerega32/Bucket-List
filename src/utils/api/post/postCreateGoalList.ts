// interface CreateGoalListResponse {
// 	success: boolean;
// 	data?: any;
// 	error?: string;
// }

export const postCreateGoalList = async (): Promise<any> => {
	// try {
	// 	const response = await api.post('/goal-lists/create/', formData, {
	// 		headers: {
	// 			'Content-Type': 'multipart/form-data',
	// 		},
	// 	});
	// 	return {
	// 		success: true,
	// 		data: response.data,
	// 	};
	// } catch (error: any) {
	// 	return {
	// 		success: false,
	// 		error: error.response?.data?.error || 'Произошла ошибка при создании списка целей',
	// 	};
	// }
};
